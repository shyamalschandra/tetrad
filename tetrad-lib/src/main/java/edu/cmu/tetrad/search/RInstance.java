package edu.cmu.tetrad.search;

//import org.rosuda.JRI.Rengine;

public class RInstance {
//    public static RInstance instance = new RInstance();
//    private org.rosuda.JRI.Rengine engine;

//    public static RInstance getInstance() {
//        return instance;
//    }

    private RInstance() {
//        System.out.println("Testing R code in Java");
//
//        System.out.println(System.getProperty("java.library.path"));
//        System.out.println(System.getProperty("R_HOME"));
//
//        // just making sure we have the right version of everything
//        if (!Rengine.versionCheck()) {
//            System.err.println("** Version mismatch - Java files don't match library version.");
//            System.exit(1);
//        }
//        System.out.println("Creating Rengine (with arguments)");
//        // 1) we pass the arguments from the command line
//        // 2) we won't use the main loop at first, we'll start it later
//        //    (that's the "false" as second argument)
//        // 3) the callbacks are implemented by the TextConsole class above
//
//        engine = new Rengine(new String[]{"--save"}, false, new TextConsole());
//        System.out.println("Rengine created, waiting for R");
//        // the engine creates R is a new thread, so we should wait until it's ready
//        if (!getEngine().waitForR()) {
//            throw new IllegalStateException("Cannot load R");
//        }
    }

//    public Rengine getEngine() {
//        return engine;
//    }
}
